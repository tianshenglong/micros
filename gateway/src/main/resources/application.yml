eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/
  instance:
    ip-address: 127.0.0.1
    prefer-ip-address: true
    instance-id: ${eureka.instance.ip-address}:${spring.application.name}:${server.port}
    status-page-url: http://${eureka.instance.ip-address}:${server.port}/actuator/info
    health-check-url: http://${eureka.instance.ip-address}:${server.port}/actuator/health
    home-page-url: http://${eureka.instance.ip-address}:${server.port}/
#logging:
#  config: classpath:log4j2.xml
#  level:
#    com:
#      study:
#        microsuser:
#          dao: debug
server:
  port: 8202
  undertow:
    accesslog:
      enabled: true
      dir: logs/server
spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: micros-user-route #·�ɵ�ID
          uri: lb://micros-user  #ƥ���·�ɵ�ַ
          predicates:  #���ԣ�·����ƥ��Ľ���·��
            - Path=/user/**
          filters:
            - StripPrefix=1
      discovery:
        locator:
          enabled: true  #表明gateway开启服务注册和发现的功能，并且spring cloud gateway自动根据服务发现为每一个服务创建了一个router，这个router将以服务名开头的请求路径转发到对应的服务。
          lower-case-service-id: true   #是将请求路径上的服务名配置为小写（因为服务注册的时候，向注册中心注册时将服务名转成大写的了），比如以/service-hi/*的请求路径被路由转发到服务名为service-hi的服务上。

#feign:
#  hystrix:
#    enabled: true
#  okhttp:
#    enabled: true
#hystrix:
#  threadpool:
#    default:
#      coreSize: 200
#  command:
#    default:
#      execution:
#        isolation:
#          thread:
#            timeoutInMilliseconds: 60000
##ʹ���۶Ͼۺϼ��ʱ��Finchley.M9 �汾��Actuator������仯�ˣ�Ĭ��ֻ��¶health�ȼ����˵���Ҫ�����������ò��� ʹ�����Ч
#management:
#  endpoints:
#    web:
#      exposure:
#        include: health,info,hystrix.stream
#  endpoint:
#    shutdown:
#      enabled: true
#    health:
#      show-details: always